# Requirements pour Transport Optimizer
# Package complet : API Python + Application Streamlit

# === Core Dependencies ===
# Données et géospatial
pandas>=1.5.0
geopandas>=0.13.0
shapely>=2.0.0
pyarrow>=10.0.0          # Pour les fichiers Parquet

# APIs et réseau
requests>=2.28.0
polyline>=1.4.0          # Décodage des géométries Geovelo

# === Streamlit Web App ===
streamlit>=1.28.0
streamlit-folium>=0.13.0  # Cartes interactives
folium>=0.14.0           # Cartes Leaflet

# === Optionnel pour développement ===
# jupyter>=1.0.0         # Notebooks (décommentez si nécessaire)
# matplotlib>=3.5.0      # Visualisations (décommentez si nécessaire)
# seaborn>=0.11.0        # Graphiques statistiques (décommentez si nécessaire)